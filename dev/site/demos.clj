(ns site.demos
  (:require [site.fabricate.adorn :as adorn]
            [site.fabricate.adorn.forms :as forms]))

;; Here's an ultimate test of how capable adorn is: use it to turn Fabricate
;; inside out. If I can directly produce a HTML page using this file and
;; adorn's extension mechanisms, then I will really know that it's ready for
;; prime time.

;; questions:
;; 1. can I override the comment mechanism for top-level comments - those
;; not inside a clojure form?
;;

;; # Demos

;; ## matching parens



;; ## highlighting all defs
;;
